import React from "react";
import Layout from "../components/common/Layout";

const NotFound = () => {
  return (
    <Layout title="Unauthorized Page">
      {/* <div className="h-[100vh]  bg-black opacity-70  grid place-items-center">
          <div className="text-black text-center bg-white drop-shadow-2xl rounded-lg p-12">
            <h1 className="text-xl text-accent text-center">Access Denied</h1>
            {message && (
              <div className="mb-4 text-error text-center">{message}</div>
            )}
            <Link className=" mt-2" href={'/'}>Go to Home Page</Link>
            <hr className="my-2" />
            <h2 className="">Or</h2>
            <hr className="my-2" />
            <Link className=" mt-4 block" href={'/login'}>Login</Link>
          </div>
        </div> */}
      this is now found
    </Layout>
  );
};

export default NotFound;
